<%- include('./components/header') %>

<body class="w-screen h-screen">
  <div class="w-full h-full max-w-screen-xl mx-auto bg-white">

   <div id="loader" class="w-full h-full overflow-hidden bg-white flex flex-col items-center justify-center">
    <div class="spinner-container mb-8">
      <div class="spinner"></div>
    </div>

    <!-- Dancing classConnect letters -->
    <div id="dancing-letters" class="flex space-x-1 text-4xl font-bold text-black">
      <span class="letter">c</span>
      <span class="letter">l</span>
      <span class="letter">a</span>
      <span class="letter">s</span>
      <span class="letter">s</span>
      <span class="letter">C</span>
      <span class="letter">o</span>
      <span class="letter">n</span>
      <span class="letter">n</span>
      <span class="letter">e</span>
      <span class="letter">c</span>
      <span class="letter">t</span>
    </div>
  </div>

   <main class="flex flex-col md:flex-row items-center justify-between px-8 py-16">
    <div class="max-w-lg mb-8 md:mb-0">
     <div class="flex items-center space-x-2 mb-4">
      <h1 class="text-4xl font-bold">
       Uniting Students and Teachers for <span class="text-blue-600">Success</span>
      </h1>
     </div>
     <p class="text-gray-600 mb-8">
      We’ll help you educate your children at home.
     </p>
     <div class="flex space-x-4">
      <a href="/student/register">
      <button class="border hover:bg-green-500 hover:text-white border-green-500 text-green-500 px-6 py-2 rounded-full">
       Are you Student?
      </button>
      </a>

      <a href="/teacher/register">
        <button class="border hover:bg-green-500 hover:text-white border-green-500 text-green-500 px-6 py-2 rounded-full">
         Are you Teacher?
        </button>
        </a>

     </div>
    </div>
    <div>
      <img src="/images/land.png" alt="">
    </div>
   </main>
   <footer class="flex flex-col md:flex-row justify-between items-center py-4 px-8 bg-white">
    <span>
     www.classconnect.com
    </span>
   </footer>
  </div>
  <script>
   document.getElementById('menu-btn').addEventListener('click', function() {
            var menu = document.getElementById('mobile-menu');
            menu.classList.toggle('hidden');
        });
  </script>

   <!-- Include GSAP Library -->
   <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.11.5/gsap.min.js"></script>

   <!-- Custom CSS for Spinner -->
   <style>
     /* Spinner container styles */
     .spinner-container {
       display: flex;
       align-items: center;
       justify-content: center;
       width: 100px;
       height: 100px;
     }
 
     /* Spinner styles */
     .spinner {
       border: 6px solid rgba(0, 0, 0, 0.1); /* Subtle light border */
       border-top: 6px solid #4A90E2; 
       border-radius: 50%;
       width: 80px;
       height: 80px;
       animation: spin 1.5s linear infinite; /* Infinite spin animation */
     }
 
     /* Spinner keyframes */
     @keyframes spin {
       0% { transform: rotate(0deg); }
       100% { transform: rotate(360deg); }
     }
   </style>
 
   <script>
     // GSAP animation for dancing letters
     gsap.fromTo(".letter", 
       {y: 0},   // Start position
       {
         y: -20,  // Move the letters upward slightly
         duration: 0.5,
         ease: "bounce.inOut",  // Bounce effect
         repeat: -1,            // Infinite loop
         stagger: 0.1,          // Delay each letter
         yoyo: true             // Animate back to original position
       });
 
     // Hide the loader and show the main content after a certain time
     setTimeout(function() {
       document.getElementById("loader").style.display = "none"; // Hide loader
       document.getElementById("main-content").style.display = "flex"; // Show main content
     }, 5000); // 5 seconds duration
   </script>
 </body>


<%- include('./components/footer') %>
